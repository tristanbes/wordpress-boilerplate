{
  "private": true,
  "scripts": {
    "build": "yprox-cli build --lint --mode production",
    "watch": "yprox-cli build --lint --watch --mode development",
    "lint": "yprox-cli lint",
    "format": "yprox-cli lint --fix",
    "vendor": "yprox-cli build --mode production --filter:_name vendor"
  },
  "devDependencies": {
    "@yproximite/eslint-config-wordpress": "^1.0.13",
    "@yproximite/stylelint-config-wordpress": "^1.0.0",
    "@yproximite/yprox-cli": "^3.3.0",
    "babel-eslint": "^10.0.0",
    "eslint": "^5.15.2",
    "prettier": "^1.16.4",
    "stylelint": "^9.10.1"
  },
  "dependencies": {},
  "browserslist": [
    "> 1%",
    "last 1 version",
    "not dead"
  ]
}
